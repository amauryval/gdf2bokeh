{% set name = "geo_bokeh" %}
{% set version = "0.5.4" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  path: ..

build:
  number: 1

requirements:
  host:
    - python >=3.6
  build:
    - python >=3.6
    - pip
    - geopandas
    - bokeh >=2.0.1
  run:
    - python >=3.6
    - geopandas
    - bokeh >=2.0.1

about:
  home: https://github.com/amauryval/geo_bokeh
  license: MIT
  license_family: MIT
  summary: geo bokeh
  license_file: LICENSE
